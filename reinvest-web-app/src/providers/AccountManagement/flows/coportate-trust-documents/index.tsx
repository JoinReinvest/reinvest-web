import { FlowIdentifiers } from '../../enums';
import { SubFlow } from '../../interfaces';
import { Flow } from './flow';

export const FLOW_DOCUMENTS_CORPORATE_TRUST: SubFlow = {
  identifier: FlowIdentifiers.DOCUMENTS,
  flow: <Flow />,
};
